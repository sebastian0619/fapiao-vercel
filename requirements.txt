# 基本依赖
fastapi==0.109.0
uvicorn==0.27.0
jinja2==3.1.3
PyPDF2==3.0.1
python-multipart==0.0.6
pillow==10.2.0
numpy==1.26.1
requests==2.31.0

# 可选依赖 - 这些可能在Vercel环境中不可用
# 如果安装失败，应用程序将降级使用备用功能
# PyMuPDF==1.23.8  # 用于PDF图像提取
# pyzbar==0.1.9    # 用于二维码读取 (需要zbar系统库)
# defusedxml==0.7.1  # 用于XML解析
# lxml==4.9.3        # 用于XML解析